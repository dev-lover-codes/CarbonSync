---
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/index.css, tailwind.config.js, postcss.config.js]
autonomous: false
requirements: [INFRA-01, INFRA-02]
---

# Plan: Scaffold Vite + React and configure Tailwind CSS theme

Objective: Pivot from Next.js to React + Vite and setup the design system.

<tasks>
<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
- package.json
</read_first>
<action>
Clean up existing Next.js structure. Install Vite, React, and required dependencies: `firebase`, `react-router-dom`, `tailwindcss`, `postcss`, `autoprefixer`, `@tailwindcss/forms`, `recharts`, `framer-motion`, `react-hot-toast`, `lucide-react`. Create a basic Vite + React scaffold in the root.
</action>
<acceptance_criteria>
- package.json contains `vite`, `react`, `react-dom` in dependencies/devDependencies.
- `npm run dev` (or equivalent vite command) starts without error.
- Next.js specific files (next.config.ts, src/app/) are removed or replaced.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
</read_first>
<action>
Initialize Tailwind CSS with `npx tailwindcss init -p`. Replace `tailwind.config.js` with the custom theme colors (primary, earth, sky, surface) and font (`Plus Jakarta Sans`) specified in CONTEXT.md.
</action>
<acceptance_criteria>
- tailwind.config.js exists and contains the custom color palette.
- tailwind.config.js content includes: `primary: { DEFAULT: "#2D6A4F", light: "#52B788", dark: "#1B4332" }`.
- `index.html` includes the Plus Jakarta Sans Google Font link.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
- src/index.css
</read_first>
<action>
Replace `src/index.css` with the Tailwind directives and CSS variables for the design system as specified in the directive.
</action>
<acceptance_criteria>
- src/index.css contains `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;`.
- src/index.css includes the `@import` for Plus Jakarta Sans.
</acceptance_criteria>
</task>
</tasks>

## Verification
- Run `npm run build` to ensure no build errors.
- Inspect `tailwind.config.js` for color accuracy.
