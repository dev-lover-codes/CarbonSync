---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tailwind.config.js, index.html, src/index.css, src/main.tsx, src/App.tsx]
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "Vite + React application is scaffolded and running"
    - "Tailwind CSS 4 is initialized with custom theme colors"
    - "Custom typography (Plus Jakarta Sans) is applied globally"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
    - path: "tailwind.config.js"
      provides: "Custom design system configuration"
    - path: "src/index.css"
      provides: "Global styles and Tailwind directives"
  key_links:
    - from: "src/main.tsx"
      to: "src/index.css"
      via: "import"
    - from: "index.html"
      to: "src/main.tsx"
      via: "script tag"
---

<objective>
Establish the React + Vite foundation and configure the design system using Tailwind CSS 4 with CarbonSync branding.

Purpose: Ensure a consistent visual identity and high-performance foundation.
Output: Working scaffold with custom theme and typography.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React & Install Dependencies</name>
  <files>package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx</files>
  <action>
    Scaffold a new Vite project using the React template if not already present.
    Install all required dependencies (per D-01):
    `npm install firebase react-router-dom tailwindcss postcss autoprefixer @tailwindcss/forms recharts framer-motion react-hot-toast lucide-react`
    Configure `vite.config.ts` for a standard React setup.
    Ensure `index.html` has the standard Vite root div and script entry.
  </action>
  <verify>
    <automated>npm list firebase react-router-dom tailwindcss && grep -q "root" index.html</automated>
  </verify>
  <done>Dependencies installed and basic Vite scaffold ready.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS 4 Design System</name>
  <files>tailwind.config.js, src/index.css, index.html</files>
  <action>
    1. Initialize Tailwind CSS: `npx tailwindcss init -p`.
    2. Replace `tailwind.config.js` with the exact configuration (per D-02):
       - content: ["./index.html", "./src/**/*.{js,jsx,ts,tsx}"]
       - Extend theme colors: primary (#2D6A4F, #52B788, #1B4332), earth (#8B5E3C, #D4A574, #5C3D1E), sky (#0EA5E9, #BAE6FD, #0369A1), surface (#F8FAF7, #FFFFFF, #0F1C14).
       - Font family: body: ['Plus Jakarta Sans', 'sans-serif'].
    3. Replace `src/index.css` with exact content (per D-03, D-04):
       - @import Plus Jakarta Sans from Google Fonts.
       - @tailwind base, components, utilities.
       - Custom CSS variables for the design system.
    4. Ensure `index.html` includes the font link (per D-04).
  </action>
  <verify>
    <automated>grep -q "primary" tailwind.config.js && grep -q "Plus Jakarta Sans" src/index.css</automated>
  </verify>
  <done>Tailwind CSS configured with custom theme and typography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Browser | Untrusted styling/UI interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tailwind.config.js | mitigate | Use exact values provided in instructions |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `npm run dev` starts without errors.
- Visual inspection of the page shows Tailwind styles being applied.
</verification>

<success_criteria>
Project is scaffolded with Vite + React and Tailwind CSS 4 is fully configured with the CarbonSync design system.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
