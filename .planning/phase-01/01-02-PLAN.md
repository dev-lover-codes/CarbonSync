---
wave: 2
depends_on: [01-01-PLAN.md]
files_modified: [src/lib/firebase.js, src/lib/firestore.js, src/config/carbonFactors.js, .env.local, .env.example, .gitignore]
autonomous: true
requirements: [INFRA-03, INFRA-04]
---

# Plan: Setup Firebase integration and Carbon calculation logic

Objective: Initialize Firebase services and implement core Firestore and footprint logic.

<tasks>
<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
</read_first>
<action>
Create `src/lib/firebase.js` with Firebase Auth and Firestore initialization using `VITE_` prefixed environment variables. Create `.env.local` with placeholders and `.env.example` with keys. Add `.env.local` to `.gitignore`.
</action>
<acceptance_criteria>
- src/lib/firebase.js exists and exports `app`, `auth`, `db`.
- .env.local contains `VITE_FIREBASE_API_KEY`, etc.
- .gitignore contains `.env.local`.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
</read_first>
<action>
Create `src/lib/firestore.js` with helper functions: `createUserProfile`, `getUserProfile`, `updateUserProfile`, `logActivity`, `getActivities`, `saveGoal`, `getGoals`, `updateGoal`, `getLeaderboard`, `saveInsight`, `getInsights`. Use Firebase Firestore SDK.
</action>
<acceptance_criteria>
- src/lib/firestore.js exists and exports all required functions.
- Functions correctly use `db` from `src/lib/firebase.js`.
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/phase-01/01-CONTEXT.md
</read_first>
<action>
Create `src/config/carbonFactors.js` with the CO2 emission factors provided (transport, energy, food, shopping, waste). Implement and export `calculateFootprint(activities)`.
</action>
<acceptance_criteria>
- src/config/carbonFactors.js contains all specified factors.
- `calculateFootprint` correctly sums up the CO2 impact based on activities.
</acceptance_criteria>
</task>
</tasks>

## Verification
- Verify `src/lib/firebase.js` initialization doesn't throw immediate errors (ignoring missing env values).
- Run a basic test or check for `calculateFootprint` logic.
