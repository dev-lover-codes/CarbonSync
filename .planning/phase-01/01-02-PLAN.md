---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/lib/firebase.js, src/lib/firestore.js, src/config/carbonFactors.js, .env.local, .env.example, .gitignore]
autonomous: true
requirements: [INFRA-03, INFRA-04]

must_haves:
  truths:
    - "Firebase is initialized with correct environment variable keys"
    - "Firestore helper functions are implemented for users, activities, goals, and insights"
    - "Carbon footprint calculation logic accurately sums CO2 impact"
  artifacts:
    - path: "src/lib/firebase.js"
      provides: "Firebase App, Auth, and Firestore instances"
    - path: "src/lib/firestore.js"
      provides: "Database abstraction layer"
    - path: "src/config/carbonFactors.js"
      provides: "Emission factors and calculation utility"
  key_links:
    - from: "src/lib/firestore.js"
      to: "src/lib/firebase.js"
      via: "importing db"
    - from: "src/config/carbonFactors.js"
      to: "feature logic"
      via: "exporting calculateFootprint"
---

<objective>
Initialize Firebase services and implement the data access layer and carbon calculation logic.

Purpose: Provide a robust data management and logic foundation for the platform features.
Output: Firebase config, Firestore helpers, and Carbon calculation utility.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firebase Initialization & Env Config</name>
  <files>src/lib/firebase.js, .env.local, .env.example, .gitignore</files>
  <action>
    1. Create `src/lib/firebase.js` (per D-05). Initialize `firebase/app`, `firebase/auth` (getAuth), and `firebase/firestore` (getFirestore).
    2. Use these exact environment variable names: `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`.
    3. Export `app`, `auth`, `db`.
    4. Create `.env.local` with all variable keys (empty values).
    5. Create `.env.example` as a reference.
    6. Ensure `.env.local` is added to `.gitignore`.
  </action>
  <verify>
    <automated>grep -q "VITE_FIREBASE_API_KEY" src/lib/firebase.js && grep -q ".env.local" .gitignore</automated>
  </verify>
  <done>Firebase initialized and environment variables configured safely.</done>
</task>

<task type="auto">
  <name>Task 2: Firestore Helpers & Carbon Logic</name>
  <files>src/lib/firestore.js, src/config/carbonFactors.js</files>
  <action>
    1. Create `src/lib/firestore.js` with these helper functions (per D-09):
       - `createUserProfile(uid, data)`
       - `getUserProfile(uid)`
       - `updateUserProfile(uid, data)`
       - `logActivity(uid, activityData)` (adds to 'activities' subcollection under user)
       - `getActivities(uid, limitCount)` (last N ordered by timestamp desc)
       - `saveGoal(uid, goalData)` (saves to 'goals' subcollection)
       - `getGoals(uid)`
       - `updateGoal(uid, goalId, data)`
       - `getLeaderboard()` (top 10 by totalSaved desc from 'users' collection)
       - `saveInsight(uid, insightData)` (saves to 'insights' subcollection)
       - `getInsights(uid)` (last 7)
    2. Create `src/config/carbonFactors.js` with exact emission factors (per D-10):
       - transport, energy, food, shopping, waste categories.
       - Export `calculateFootprint(activities)` function that sums CO2 values based on these factors.
  </action>
  <verify>
    <automated>grep -q "createUserProfile" src/lib/firestore.js && grep -q "calculateFootprint" src/config/carbonFactors.js</automated>
  </verify>
  <done>Data access layer and business logic for carbon tracking implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Untrusted writes (mitigated by rules, to be added later) |
| Environment → App | Sensitive config loaded via process.env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .env.local | mitigate | Added to .gitignore in Task 1 |
| T-02-02 | Tampering | carbonFactors.js | mitigate | Values locked by PRD/Discovery |
</threat_model>

<verification>
- Static analysis of helper functions confirms they use `db` correctly.
- `calculateFootprint` can be manually verified with a test activity object.
</verification>

<success_criteria>
Firebase is integrated, Firestore abstractions are ready, and carbon calculation logic is implemented and tested.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
