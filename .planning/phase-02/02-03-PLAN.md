---
phase: 02-user-identity
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/app/profile/page.tsx, src/lib/actions/profile.ts, src/lib/actions/profile.test.ts]
autonomous: true
requirements: [PROF-01, PROF-02]

must_haves:
  truths:
    - "Users can view their profile data on /profile (PROF-01)"
    - "Users can update their profile via Server Action (PROF-02)"
  artifacts:
    - path: "src/app/profile/page.tsx"
      provides: "Profile UI"
    - path: "src/lib/actions/profile.ts"
      provides: "Profile Server Actions"
  key_links:
    - from: "src/app/profile/page.tsx"
      to: "src/lib/actions/profile.ts"
      via: "form action import"
    - from: "src/lib/actions/profile.ts"
      to: "Firebase Admin SDK"
      via: "adminDb usage"
---

<objective>
Implement user profile management, including a protected UI for viewing and updating user data persisted in Firestore.

Purpose: Enables users to manage their identity and persists user metadata per PROF-01 and PROF-02.
Output: Firestore profile integration and a functional `/profile` page.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02/02-CONTEXT.md
@.planning/phase-02/02-VALIDATION.md
@.planning/phase-02/RESEARCH.md
@.planning/phases/02-user-identity/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Profile Server Actions</name>
  <files>src/lib/actions/profile.ts</files>
  <action>
    - Create `src/lib/actions/profile.ts` with `"use server"` directive.
    - Implement `getProfile` utility to fetch user data from Firestore using the Firebase Admin SDK (PROF-01).
    - Implement `updateProfile` action that validates input with `zod` and updates Firestore document at `users/{uid}` per D-02 and D-03 (PROF-02).
    - Ensure the `uid` is retrieved from the verified session (D-05).
  </action>
  <verify>
    <automated>ls src/lib/actions/profile.ts && npm run lint</automated>
  </verify>
  <done>Server Actions for profile management are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build Profile Page UI</name>
  <files>src/app/profile/page.tsx</files>
  <action>
    - Create `src/app/profile/page.tsx` as a protected route per D-05.
    - Use `getProfile` to fetch data and render it in a form.
    - Use React 19 `useActionState` to handle the `updateProfile` action and show loading/success/error states per D-03.
    - Style with Tailwind CSS 4.
  </action>
  <verify>
    <automated>ls src/app/profile/page.tsx && npm run lint</automated>
  </verify>
  <done>The protected profile page is functional and styled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test Profile Actions</name>
  <files>src/lib/actions/profile.test.ts</files>
  <behavior>
    - Test `getProfile`: retrieves the correct document for a given UID.
    - Test `updateProfile`: updating valid fields succeeds in Firestore.
    - Test `updateProfile`: invalid fields fail validation.
  </behavior>
  <action>
    - Create `src/lib/actions/profile.test.ts` per 02-VALIDATION.md.
    - Implement tests for the profile Server Actions, mocking Firestore/Firebase Admin SDK.
  </action>
  <verify>
    <automated>npm test src/lib/actions/profile.test.ts && npm run lint</automated>
  </verify>
  <done>Profile management logic is verified via automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Server Action | Data from user profile form enters the Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Profile Data | mitigate | Use `zod` for strict schema validation in Server Actions. |
| T-02-06 | Elevation of Privilege | Profile Update | mitigate | Ensure the `uid` used for Firestore update comes from the verified session, not user input. |
</threat_model>

<verification>
Run `npm test src/lib/actions/profile.test.ts` and `npm run lint`.
</verification>

<success_criteria>
Users can successfully view and update their profile, changes persist in Firestore, and tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-user-identity/02-03-SUMMARY.md` when done
</output>
