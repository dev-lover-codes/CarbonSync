---
phase: 02-user-identity
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/app/profile/page.tsx, src/lib/actions/profile.ts, src/components/profile/ProfileForm.tsx]
autonomous: false
requirements: [PROF-01, PROF-02]

must_haves:
  truths:
    - "User profile document is automatically created in Firestore"
    - "Only authenticated users can access the profile page"
    - "User can update their displayName and photoURL via the profile page"
  artifacts:
    - path: "src/app/profile/page.tsx"
      provides: "Protected Profile UI"
    - path: "src/lib/actions/profile.ts"
      provides: "Profile mutation Server Actions"
  key_links:
    - from: "src/app/profile/page.tsx"
      to: "src/lib/actions/profile.ts"
      via: "form action attribute"
---

<objective>
Implement Firestore-backed user profiles and protected routing. Create a profile management interface where users can view and update their identity metadata.

Purpose: Persists user-specific data and demonstrates protected route access.
Output: Firestore 'users' collection with profile documents, a protected /profile page, and update Server Actions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02/02-CONTEXT.md
@.planning/phase-02/RESEARCH.md
@.planning/phase-02/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Profile Creation and Update Server Actions</name>
  <files>src/lib/actions/profile.ts</files>
  <action>
    - Define a Zod schema for profile validation (displayName, photoURL).
    - Create a `syncUserProfile` Server Action that ensures a user document exists in Firestore (checks `users/{uid}`). Call this after login/signup or in the profile page load.
    - Create an `updateUserProfile` Server Action using the `"use server"` directive.
    - Reference D-03 (Server Actions) in CONTEXT.md for React 19 patterns.
    - Use `firebase-admin` to perform these mutations on the server.
  </action>
  <verify>
    <automated>grep "'use server'" src/lib/actions/profile.ts</automated>
  </verify>
  <done>Server Actions for profile management are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Create Protected Profile Page</name>
  <files>src/app/profile/page.tsx, src/components/profile/ProfileForm.tsx</files>
  <action>
    - Create `src/app/profile/page.tsx` as a Server Component.
    - Retrieve user session from headers/context (provided by middleware/next-firebase-auth-edge).
    - If no session, the middleware should have already redirected, but add a fallback check.
    - Fetch the user profile from Firestore using `adminDb`.
    - Create `src/components/profile/ProfileForm.tsx` using `useActionState` (React 19) to handle the `updateUserProfile` action.
    - Implement the UI with Tailwind CSS 4, including loading states and success/error notifications.
  </action>
  <verify>
    <automated>ls src/app/profile/page.tsx src/components/profile/ProfileForm.tsx</automated>
  </verify>
  <done>Protected profile page with update functionality is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Protected Profile page with Firestore persistence.</what-built>
  <how-to-verify>
    1. Log in to the application.
    2. Navigate to `/profile`.
    3. Update your display name and save.
    4. Refresh the page to ensure the change persisted.
    5. Attempt to visit `/profile` in an Incognito window (unauthenticated) and verify you are redirected to `/login`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Firestore | Server-side mutations; assumes identity from session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Elevation of Privilege | Profile Update | mitigate | Ensure the `updateUserProfile` Server Action validates that the `uid` being updated matches the `uid` in the verified session cookie. |
| T-02-06 | Tampering | Firestore Data | mitigate | Configure Firestore Security Rules (outside of this plan, but noted) to allow only owners to write to their documents. |
</threat_model>

<verification>
Check Firestore console to verify 'users' collection contains the expected profile document for the test user.
</verification>

<success_criteria>
- User profiles are created/retrieved in Firestore.
- `/profile` route is only accessible to authenticated users.
- Profile updates reflect in both Firestore and the UI.
</success_criteria>

<output>
Create `.planning/phases/02-user-identity/02-03-SUMMARY.md` when done
</output>
