---
phase: 02-user-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/lib/firebase/client.ts, src/lib/firebase/admin.ts, src/middleware.ts, src/app/api/login/route.ts, src/app/api/logout/route.ts]
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "Firebase Client and Admin SDKs are correctly initialized"
    - "Middleware intercepts requests and handles session cookies"
    - "API routes for login/logout successfully set/clear cookies"
  artifacts:
    - path: "src/lib/firebase/client.ts"
      provides: "Firebase Client instance"
    - path: "src/lib/firebase/admin.ts"
      provides: "Firebase Admin instance"
    - path: "src/middleware.ts"
      provides: "Edge-compatible session management"
  key_links:
    - from: "src/middleware.ts"
      to: "next-firebase-auth-edge"
      via: "authMiddleware import"
    - from: "src/app/api/login/route.ts"
      to: "next-firebase-auth-edge"
      via: "setSession helper"
    - from: "src/app/api/logout/route.ts"
      to: "next-firebase-auth-edge"
      via: "removeSession helper"
---

<objective>
Setup the foundational authentication infrastructure using Firebase SDKs and next-firebase-auth-edge to support secure, server-side session management.

Purpose: Enables authenticated routes and secure data access in Server Components per D-01.
Output: Initialized Firebase SDKs, Auth Middleware, and session management API routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02/02-CONTEXT.md
@.planning/phase-02/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Authentication Dependencies</name>
  <files>package.json</files>
  <action>
    Install core authentication and validation libraries per RESEARCH.md:
    - `firebase`: Client SDK for auth and firestore.
    - `firebase-admin`: Server SDK for protected operations.
    - `next-firebase-auth-edge`: For session handling in Middleware (implements D-01).
    - `zod`: For schema validation.
    
    Ensure `npm install` is executed.
  </action>
  <verify>
    <automated>grep -E "firebase|firebase-admin|next-firebase-auth-edge|zod" package.json && npm run lint</automated>
  </verify>
  <done>Dependencies installed and recorded in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Firebase Client and Admin SDKs</name>
  <files>src/lib/firebase/client.ts, src/lib/firebase/admin.ts</files>
  <action>
    - Create `src/lib/firebase/client.ts` to export `auth` and `db` using `getApps()`, `getApp()`, `initializeApp()`.
    - Create `src/lib/firebase/admin.ts` to export `adminAuth` and `adminDb` using `firebase-admin/app` and `firebase-admin/auth`. Use environment variables for project configuration.
    - This foundational setup supports the implementation of D-01.
  </action>
  <verify>
    <automated>ls src/lib/firebase/client.ts src/lib/firebase/admin.ts && npm run lint</automated>
  </verify>
  <done>Firebase SDKs configured for both client and server environments.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auth Middleware and Session API Routes</name>
  <files>src/middleware.ts, src/app/api/login/route.ts, src/app/api/logout/route.ts</files>
  <action>
    - Implement `src/middleware.ts` using `authMiddleware` from `next-firebase-auth-edge` to enforce route protection per D-05.
    - Create `src/app/api/login/route.ts` to handle session cookie creation via `next-firebase-auth-edge` API helpers, ensuring HTTP-only security per D-04.
    - Create `src/app/api/logout/route.ts` to clear session cookies.
  </action>
  <verify>
    <automated>ls src/middleware.ts src/app/api/login/route.ts src/app/api/logout/route.ts && npm run lint</automated>
  </verify>
  <done>Middleware and session API routes are operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted ID tokens from Firebase Client SDK are sent to `/api/login` |
| Middleware → App | Middleware injects verified session into headers/request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Auth Session | mitigate | Use `next-firebase-auth-edge` for secure cookie validation and rotation (D-04). |
| T-02-02 | Information Disclosure | Session Token | mitigate | Ensure cookies are `httpOnly`, `secure`, and `sameSite: "lax"` (D-04). |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that `npm run lint` passes and that Firebase configuration files exist.
</verification>

<success_criteria>
Middleware correctly routes unauthenticated users away from protected areas (though none exist yet, the structure is there).
</success_criteria>

<output>
Create `.planning/phases/02-user-identity/02-01-SUMMARY.md` when done
</output>
