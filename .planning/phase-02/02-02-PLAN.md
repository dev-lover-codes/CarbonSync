---
phase: 02-user-identity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/(auth)/signup/page.tsx, src/app/(auth)/login/page.tsx, src/components/auth/AuthForm.tsx]
autonomous: false
requirements: [AUTH-01]

must_haves:
  truths:
    - "User can sign up with email and password"
    - "User can log in with valid credentials"
    - "Successful login redirects to dashboard and sets session cookie"
  artifacts:
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Registration UI"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login UI"
  key_links:
    - from: "src/components/auth/AuthForm.tsx"
      to: "/api/login"
      via: "fetch POST"
---

<objective>
Implement the Signup and Login user interfaces and the client-side logic to authenticate with Firebase and establish a server-side session.

Purpose: Provides the user entry point for identity management.
Output: Functional Login and Signup pages with validation and session integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02/02-CONTEXT.md
@.planning/phase-02/RESEARCH.md
@.planning/phase-02/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Signup Page and Component</name>
  <files>src/app/(auth)/signup/page.tsx, src/components/auth/AuthForm.tsx</files>
  <action>
    - Create a reusable `AuthForm` component in `src/components/auth/AuthForm.tsx` that handles email/password inputs using Tailwind CSS 4 styles.
    - Implement `src/app/(auth)/signup/page.tsx` using the `AuthForm`.
    - Logic: Use `createUserWithEmailAndPassword` from Firebase Client SDK. On success, call `fetch('/api/login', ...)` with the user's ID token to set the session cookie.
    - Redirect to `/` or `/profile` on success.
  </action>
  <verify>
    <automated>ls src/app/(auth)/signup/page.tsx src/components/auth/AuthForm.tsx</automated>
  </verify>
  <done>Signup flow is implemented and integrated with session API.</done>
</task>

<task type="auto">
  <name>Task 2: Create Login Page</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
    - Implement `src/app/(auth)/login/page.tsx` using the `AuthForm`.
    - Logic: Use `signInWithEmailAndPassword` from Firebase Client SDK. On success, call `fetch('/api/login', ...)` with the user's ID token.
    - Handle errors (invalid credentials, etc.) and display user-friendly messages.
    - Ensure the layout is mobile-responsive per GEMINI.md guidelines.
  </action>
  <verify>
    <automated>ls src/app/(auth)/login/page.tsx</automated>
  </verify>
  <done>Login flow is implemented and integrated with session API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authentication pages (Login/Signup) with Firebase integration.</what-built>
  <how-to-verify>
    1. Run the dev server (if not already running).
    2. Navigate to `/signup` and create a test user.
    3. Verify you are redirected after successful signup.
    4. Check browser cookies to confirm the session cookie is set.
    5. Navigate to `/login`, log out (manually or via a button if present), and log back in.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Client UI | Raw credentials entered into browser forms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | Login Form | mitigate | Use Firebase Auth's built-in protection; ensure HTTPS is used for all traffic. |
| T-02-04 | Information Disclosure | Auth Errors | mitigate | Do not reveal if email exists vs password wrong (use generic "Invalid credentials" message). |
</threat_model>

<verification>
Check browser dev tools for successful POST to `/api/login` and presence of session cookie.
</verification>

<success_criteria>
- Signup creates a user in Firebase Auth.
- Login authenticates existing users.
- Both flows correctly trigger session cookie creation.
</success_criteria>

<output>
Create `.planning/phases/02-user-identity/02-02-SUMMARY.md` when done
</output>
