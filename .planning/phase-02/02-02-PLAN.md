---
phase: 02-user-identity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx, src/components/auth/AuthForm.tsx, src/app/(auth)/auth.test.ts]
autonomous: true
requirements: [AUTH-01, PROF-01]

must_haves:
  truths:
    - "User can view login and signup forms"
    - "Signup form creates a new user via Firebase Client SDK"
    - "New users get a Firestore profile document on signup (PROF-01)"
    - "Login form authenticates user and initiates session creation via /api/login (Bridge Integration)"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login route"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup route"
    - path: "src/components/auth/AuthForm.tsx"
      provides: "Reusable auth form component"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "/api/login"
      via: "fetch POST with ID token"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "/api/login"
      via: "fetch POST with ID token"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "Firestore users/{uid}"
      via: "setDoc (initial profile creation)"
---

<objective>
Implement the user-facing authentication flows (Login and Signup) using modern React 19 patterns and Tailwind CSS 4, ensuring full integration between client-side auth and server-side sessions.

Purpose: Provides the entry point for users to authenticate with the application per AUTH-01 and ensures profile persistence per PROF-01.
Output: Functional Login and Signup pages with validation, automated profile creation, and session bridging.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-02/02-CONTEXT.md
@.planning/phase-02/02-VALIDATION.md
@.planning/phase-02/RESEARCH.md
@.planning/phases/02-user-identity/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Auth UI and Integrate Logic</name>
  <files>src/components/auth/AuthForm.tsx, src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <action>
    - Create a reusable `AuthForm` component using Tailwind CSS 4 for styling.
    - Implement `src/app/(auth)/login/page.tsx` and `src/app/(auth)/signup/page.tsx` using the `AuthForm`.
    - Use React 19 `useActionState` for form state management per D-03.
    - **Bridge Session Integration**: After successful authentication via Firebase Client SDK (`signInWithEmailAndPassword` or `createUserWithEmailAndPassword`), fetch the ID token using `user.getIdToken()` and send it to `/api/login` via POST to establish the server-side session cookie (D-04).
    - **Automated Profile Creation (PROF-01)**: Upon successful signup, immediately initialize a Firestore document at `users/{uid}` with fields: `email`, `displayName`, `photoURL`, `createdAt`, and `updatedAt` (D-02).
  </action>
  <verify>
    <automated>ls src/components/auth/AuthForm.tsx src/app/(auth)/login/page.tsx src/app/(auth)/signup/page.tsx && npm run lint</automated>
  </verify>
  <done>Authentication UI pages are created, session bridging is implemented, and profile creation is wired into the signup flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and Test Auth Logic</name>
  <files>src/app/(auth)/auth.test.ts</files>
  <behavior>
    - Test successful login flow: valid credentials return success and trigger `/api/login` call.
    - Test failed login flow: invalid credentials return appropriate error messages.
    - Test signup flow: creating a new user succeeds, triggers Firestore profile creation (PROF-01), and triggers `/api/login` call.
  </behavior>
  <action>
    - Create `src/app/(auth)/auth.test.ts` per 02-VALIDATION.md.
    - Implement tests for the authentication logic, mocking Firebase Client SDK calls and Firestore interactions.
  </action>
  <verify>
    <automated>npm test src/app/(auth)/auth.test.ts && npm run lint</automated>
  </verify>
  <done>Authentication logic and its side effects (profile creation, session bridging) are verified via automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Client | User input (email/password) enters the system |
| Client → API | ID Token sent to /api/login for session creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | Login Error | mitigate | Use generic error messages for authentication failures to prevent account enumeration. |
| T-02-04 | Denial of Service | Login Form | mitigate | Firebase Auth provides built-in rate limiting; ensure UI handles "too-many-requests" errors. |
</threat_model>

<verification>
Run `npm test src/app/(auth)/auth.test.ts` and `npm run lint`.
</verification>

<success_criteria>
Auth forms are functional, session cookies are set on login/signup, new users get Firestore profiles, styles are consistent with Tailwind 4, and tests pass.
</success_criteria>

<output>
Create `.planning/phases/02-user-identity/02-02-SUMMARY.md` when done
</output>
