---
phase: 06-community-ai
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [package.json, src/lib/ai-service.js, src/components/ai/EcoCoach.tsx, src/components/ai/InsightCard.tsx, src/pages/InsightsPage.tsx]
autonomous: false
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "EcoCoach chatbot responds with contextually aware sustainability tips"
    - "Weekly analysis summarizes activity trends into actionable insights"
    - "The chat UI supports streaming responses from Gemini"
  artifacts:
    - path: "src/lib/ai-service.js"
      provides: "Gemini integration logic and system prompt engineering"
    - path: "src/components/ai/EcoCoach.tsx"
      provides: "AI chat assistant interface"
  key_links:
    - from: "src/components/ai/EcoCoach.tsx"
      to: "src/lib/ai-service.js"
      via: "Vercel AI SDK chat interface"
      pattern: "useChat|chatWithCoach"
    - from: "src/pages/InsightsPage.tsx"
      to: "src/lib/ai-service.js"
      via: "AI analysis function"
      pattern: "generateInsights"
---

<objective>
Integrate Gemini-powered AI intelligence to provide personalized sustainability coaching (EcoCoach) and weekly activity insights.
Purpose: Leverage AI to transform user data into actionable, personalized guidance that drives meaningful behavior change.
Output: EcoCoach chat interface and a weekly insights dashboard.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phase-06/06-CONTEXT.md
@.planning/phases/06-community-ai/06-RESEARCH.md
# Reference Phase 1 lib for Firebase/Carbon logic
@src/lib/firebase.js
@src/config/carbonFactors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup AI SDK & Gemini Configuration</name>
  <files>package.json, src/lib/ai-service.js</files>
  <action>
    Configure the AI integration layer:
    - Install `assistant-ui` (or equivalent lightweight chat UI): `npm install @assistant-ui/react`.
    - Create `src/lib/ai-service.js`:
      - Initialize the Gemini client using Vercel AI SDK (`@ai-sdk/google`).
      - Define `systemPrompt`: Inject "EcoCoach" persona and carbon factor context.
      - Implement `generateInsights(userActivities)`: A function that takes the last 7 days of logs and returns a summary + 3 tips.
      - Implement `chatWithCoach(messages, context)`: Wrapper for streaming responses.
    - Note: In a client-side Vite app, ensure `GOOGLE_GENERATIVE_AI_API_KEY` is handled via `import.meta.env.VITE_GEMINI_API_KEY`.
  </action>
  <verify>
    <automated>npm list @ai-sdk/google && grep "systemPrompt" src/lib/ai-service.js</automated>
  </verify>
  <done>AI service layer configured with Gemini and Vercel AI SDK.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Implement EcoCoach Chat UI</name>
  <files>src/components/ai/EcoCoach.tsx, src/pages/InsightsPage.tsx</files>
  <action>
    Build the AI interaction interface:
    1. `EcoCoach.tsx`:
       - Use `@assistant-ui/react` (Thread/Message) to create a floating or dedicated chat window.
       - Bind to the `chatWithCoach` service.
       - Inject user's recent activities as context in the first message (hidden or system).
    2. `InsightsPage.tsx`:
       - Create a layout for "Weekly Analysis".
       - Trigger `generateInsights` on mount (or retrieve from a cached Firestore collection `userInsights`).
       - Render `InsightCard.tsx` components with Gemini-generated content.
  </action>
  <what-built>EcoCoach chat assistant and Insights generation page.</what-built>
  <how-to-verify>
    1. Navigate to /insights.
    2. Open the EcoCoach chat.
    3. Ask "How can I reduce my transport footprint based on my recent logs?"
    4. Verify that the response references actual user data (if present) or provides specific EcoCoach persona advice.
  </how-to-verify>
  <resume-signal>Chat responds and insights load correctly</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Gemini API | API key is exposed on client-side (VITE_*). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | Gemini API | accept | Monitor token usage; Implement client-side rate limiting if needed. |
| T-06-04 | Information Disclosure | User Logs -> LLM | mitigate | Ensure no PII (email/name) is sent in the prompt context; only send activity types and values. |
| T-06-05 | Elevation of Privilege | Gemini API Key | accept | **Security Awareness Note:** Client-side API key exposure is a hackathon-only compromise. For production, move integration to a secure backend (e.g., Firebase Functions) to prevent key exposure. |
</threat_model>

<verification>
- Verify Gemini response streaming in the chat UI.
- Verify that "Weekly Insights" are personalized to the user's activity trends.
</verification>

<success_criteria>
- EcoCoach provides relevant sustainability advice.
- AI-generated insights appear on the Insights page.
- Chat UI is responsive and handles streaming text correctly.
</success_criteria>

<output>
Create .planning/phases/06-community-ai/06-02-SUMMARY.md when done
</output>
