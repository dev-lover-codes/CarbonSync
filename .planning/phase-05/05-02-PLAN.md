---
phase: 05-insights-leaderboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/insights/InsightsGrid.tsx
  - src/components/insights/InsightCard.tsx
  - src/components/leaderboard/LeaderboardList.tsx
  - src/components/leaderboard/LeaderboardItem.tsx
  - src/app/dashboard/page.tsx
  - src/app/leaderboard/page.tsx
autonomous: true
requirements: [INS-01, LEAD-01]

must_haves:
  truths:
    - "User can view a grid of environmental insights"
    - "User can view a ranked leaderboard of top 10 savers"
  key_links:
    - from: "src/components/insights/InsightsGrid.tsx"
      to: "src/lib/firebase/insights.ts"
      via: "getInsights() call"
    - from: "src/components/leaderboard/LeaderboardList.tsx"
      to: "src/lib/firebase/leaderboard.ts"
      via: "getTopRankings() call"
---

<objective>
Build the user interface for Insights and Leaderboard.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create Insights Components</name>
  <files>src/components/insights/InsightCard.tsx, src/components/insights/InsightsGrid.tsx</files>
  <action>
    Build InsightCard (renders title, desc, icon based on category) and InsightsGrid (uses getInsights to fetch and render cards in a Bento-style grid using Tailwind).
  </action>
  <verify>
    <automated>ls src/components/insights/InsightCard.tsx src/components/insights/InsightsGrid.tsx && npm run lint -- --fix</automated>
  </verify>
  <done>Insights UI created and linted.</done>
</task>

<task type="auto">
  <name>Task 2: Create Leaderboard Components</name>
  <files>src/components/leaderboard/LeaderboardItem.tsx, src/components/leaderboard/LeaderboardList.tsx</files>
  <action>
    Build LeaderboardItem (renders rank, avatar, name, CO2 saved) and LeaderboardList (uses getTopRankings to fetch and render the top 10).
  </action>
  <verify>
    <automated>ls src/components/leaderboard/LeaderboardItem.tsx src/components/leaderboard/LeaderboardList.tsx && npm run lint -- --fix</automated>
  </verify>
  <done>Leaderboard UI created and linted.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Pages</name>
  <files>src/app/leaderboard/page.tsx</files>
  <action>
    Create src/app/leaderboard/page.tsx for the full rankings and update src/app/dashboard/page.tsx to render the InsightsGrid component.
  </action>
  <verify>
    <automated>ls src/app/leaderboard/page.tsx && npm run lint -- --fix</automated>
  </verify>
  <done>Pages implemented and linted.</done>
</task>

</tasks>
