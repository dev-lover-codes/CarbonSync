---
phase: 05-insights-leaderboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/firebase/insights.ts
  - src/lib/firebase/leaderboard.ts
  - src/types/index.ts
  - src/lib/firebase/seed-data.ts
autonomous: true
requirements: [INS-01, LEAD-01]

must_haves:
  truths:
    - "Can retrieve top 10 users by carbon savings from Firestore"
    - "Can retrieve curated environmental insights from Firestore"
    - "Insights have categories and impact scores"
  artifacts:
    - path: "src/lib/firebase/insights.ts"
      provides: "Functions to fetch insights"
    - path: "src/lib/firebase/leaderboard.ts"
      provides: "Functions to fetch top 10 rankings"
    - path: "src/lib/firebase/seed-data.ts"
      provides: "Initial data population for insights"
  key_links:
    - from: "src/lib/firebase/leaderboard.ts"
      to: "profiles collection"
      via: "query(collection(db, 'profiles'), orderBy('totalSavedCO2', 'desc'), limit(10))"
    - from: "src/lib/firebase/insights.ts"
      to: "insights collection"
      via: "query(collection(db, 'insights'), orderBy('dateAdded', 'desc'))"
---

<objective>
Establish the data layer for the Insights and Leaderboard features.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define Insights and Leaderboard Types</name>
  <files>src/types/index.ts</files>
  <action>
    Add the following interfaces to src/types/index.ts:
    - Insight: { id: string, title: string, description: string, category: 'transport'|'energy'|'food'|'shopping'|'waste', impactScore: number, dateAdded: any }
    - LeaderboardEntry: { uid: string, displayName: string, photoURL?: string, totalSavedCO2: number }
  </action>
  <verify>
    <automated>grep -E "interface Insight|interface LeaderboardEntry" src/types/index.ts && npm run lint -- --fix</automated>
  </verify>
  <done>Types defined and linted.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore Helpers</name>
  <files>src/lib/firebase/insights.ts, src/lib/firebase/leaderboard.ts</files>
  <action>
    Implement getInsights and getTopRankings using the patterns from RESEARCH.md.
    - getInsights should fetch from the 'insights' collection, ordered by dateAdded desc.
    - getTopRankings should fetch from 'profiles', ordered by totalSavedCO2 desc, limit 10.
  </action>
  <verify>
    <automated>grep -rE "getInsights|getTopRankings" src/lib/firebase/ && npm run lint -- --fix</automated>
  </verify>
  <done>Helpers implemented and linted.</done>
</task>

<task type="auto">
  <name>Task 3: Create and Execute Seed Script</name>
  <files>src/lib/firebase/seed-data.ts</files>
  <action>
    Implement seedInsights() to populate the 'insights' collection with 5+ items. 
    Ensure it checks for existing data before adding.
  </action>
  <verify>
    <automated>ls src/lib/firebase/seed-data.ts && npm run lint -- --fix</automated>
  </verify>
  <done>Seed logic implemented and linted.</done>
</task>

</tasks>
