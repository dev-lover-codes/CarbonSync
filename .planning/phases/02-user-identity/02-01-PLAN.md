---
phase: 02-user-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/firebase/admin.ts, src/lib/firebase/client.ts, src/middleware.ts, src/lib/auth-config.ts, .env.local]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Firebase Admin SDK initializes successfully on server"
    - "Next.js Middleware redirects unauthenticated users from /profile to /login"
    - "Firebase Client SDK is available for browser-side auth"
  artifacts:
    - path: "src/lib/firebase/admin.ts"
      provides: "Server-side Firebase Admin SDK access"
    - path: "src/lib/firebase/client.ts"
      provides: "Client-side Firebase SDK access"
    - path: "src/middleware.ts"
      provides: "Session validation and route protection"
  key_links:
    - from: "src/middleware.ts"
      to: "next-firebase-auth-edge"
      via: "authMiddleware"
---

<objective>
Establish the secure authentication foundation using next-firebase-auth-edge and Firebase Admin SDK.
Purpose: Provide server-side session management and route protection for the Next.js App Router.
Output: Initialized SDKs and functional Middleware.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Firebase SDKs and Environment Variables</name>
  <files>src/lib/firebase/admin.ts, src/lib/firebase/client.ts, .env.local</files>
  <action>
    - Install dependencies if missing: 'firebase', 'firebase-admin', 'next-firebase-auth-edge'.
    - Configure .env.local with placeholder values for:
      - NEXT_PUBLIC_FIREBASE_API_KEY, AUTH_DOMAIN, PROJECT_ID, etc.
      - FIREBASE_ADMIN_PROJECT_ID, FIREBASE_ADMIN_CLIENT_EMAIL, FIREBASE_ADMIN_PRIVATE_KEY.
    - Create src/lib/firebase/admin.ts: Export a 'getAdminApp' function that initializes the Admin SDK using service account credentials.
    - Create src/lib/firebase/client.ts: Initialize and export 'auth', 'db' from the Firebase Client SDK.
  </action>
  <verify>
    <automated>npm run lint && ls src/lib/firebase/admin.ts src/lib/firebase/client.ts</automated>
  </verify>
  <done>Firebase SDKs configured and environment variables declared.</done>
</task>

<task type="auto">
  <name>Task 2: Configure next-firebase-auth-edge and Middleware</name>
  <files>src/lib/auth-config.ts, src/middleware.ts</files>
  <action>
    - Create src/lib/auth-config.ts: Export common configuration for next-firebase-auth-edge (cookie settings, keys).
    - Create src/middleware.ts: Use 'authMiddleware' from next-firebase-auth-edge.
    - Implement route protection (D-05): Redirect unauthenticated users to '/login' if they access '/profile', '/dashboard', or '/settings'.
    - Use the 'handleInvalidToken' and 'handleValidToken' callbacks to manage redirections.
  </action>
  <verify>
    <automated>npm run lint && grep "authMiddleware" src/middleware.ts</automated>
  </verify>
  <done>Middleware configured for session validation and route protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Middleware | ID token exchanged for session cookie; Middleware validates cookie signatures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Auth Session | mitigate | Use next-firebase-auth-edge for secure cookie signing and verification |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that the Middleware correctly intercepts a request to /profile and redirects to /login when no session cookie is present.
</verification>

<success_criteria>
Firebase Admin/Client SDKs initialized, Middleware active with route protection.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-01-SUMMARY.md when done
</output>
