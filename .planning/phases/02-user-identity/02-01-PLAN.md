---
phase: 02-user-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ui/Button.jsx, src/components/ui/Card.jsx, src/components/ui/Input.jsx, src/components/ui/Badge.jsx, src/components/ui/Avatar.jsx, src/components/ui/ProgressBar.jsx, src/components/ui/Spinner.jsx, src/components/ui/Modal.jsx]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Reusable UI components are available for the Auth and Onboarding pages"
    - "Components follow the design guidelines (clean spacing, rounded-2xl, subtle shadows)"
  artifacts:
    - path: "src/components/ui/Button.jsx"
      provides: "Themed button with variants and loading state"
    - path: "src/components/ui/Card.jsx"
      provides: "White card with rounded-2xl and shadow"
    - path: "src/components/ui/Input.jsx"
      provides: "Styled input with label and error state"
    - path: "src/components/ui/Badge.jsx"
      provides: "Pill badges with color variants"
    - path: "src/components/ui/Avatar.jsx"
      provides: "Circular avatar with level ring"
    - path: "src/components/ui/ProgressBar.jsx"
      provides: "Animated progress bar"
    - path: "src/components/ui/Spinner.jsx"
      provides: "Centered loading spinner"
    - path: "src/components/ui/Modal.jsx"
      provides: "Z-indexed overlay modal"
  key_links:
    - from: "src/pages/auth/Login.jsx"
      to: "src/components/ui/Button.jsx"
      via: "Import"
    - from: "src/components/ui/Modal.jsx"
      to: "framer-motion"
      via: "Animation"
---

<objective>
Create the foundational reusable UI components for CarbonSync.
Purpose: Ensure a consistent, polished look and feel across the application and speed up development of subsequent pages.
Output: A set of 8 reusable React components in src/components/ui/.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
@GEMINI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Basic UI Components (Button, Card, Input, Badge)</name>
  <files>src/components/ui/Button.jsx, src/components/ui/Card.jsx, src/components/ui/Input.jsx, src/components/ui/Badge.jsx</files>
  <action>
    - Create src/components/ui/ folder.
    - Button.jsx: Implement variants (primary: emerald-600, secondary: outline, ghost, danger: red-600), sizes (sm, md, lg), and a loading state with a spinner. per D-03.
    - Card.jsx: Implement a white card with rounded-2xl, subtle shadow (shadow-lg), and padding variants. per D-03.
    - Input.jsx: Implement a styled input with label, error message display, and icon support (lucide-react). per D-03.
    - Badge.jsx: Implement pill-shaped badges with color variants (green, amber, blue, gray, red). per D-03.
  </action>
  <verify>
    <automated>npm run lint && grep -r "export" src/components/ui/{Button,Card,Input,Badge}.jsx</automated>
  </verify>
  <done>Basic UI components created and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create Advanced UI Components (Avatar, ProgressBar, Spinner, Modal)</name>
  <files>src/components/ui/Avatar.jsx, src/components/ui/ProgressBar.jsx, src/components/ui/Spinner.jsx, src/components/ui/Modal.jsx</files>
  <action>
    - Avatar.jsx: Circular avatar with initials fallback and a ring indicator for the user's level. per D-03.
    - ProgressBar.jsx: Animated progress bar using Tailwind/framer-motion, supporting percentage and color variants. per D-03.
    - Spinner.jsx: A simple, centered loading spinner in emerald green. per D-03.
    - Modal.jsx: A centered overlay modal using z-index: 50. Use a backdrop-blur and framer-motion. per D-03.
  </action>
  <verify>
    <automated>npm run lint && grep -r "export" src/components/ui/{Avatar,ProgressBar,Spinner,Modal}.jsx</automated>
  </verify>
  <done>Advanced UI components created and exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Pure UI components)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | UI Components | accept | Standard component development |
</threat_model>

<verification>
Manual visual check of components in the browser preview. Verify responsiveness and design fidelity.
</verification>

<success_criteria>
All 8 components are implemented, exported, and pass linting.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-01-SUMMARY.md when done
</output>
