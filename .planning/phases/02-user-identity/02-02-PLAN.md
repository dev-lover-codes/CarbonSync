---
phase: 02-user-identity
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/contexts/AuthContext.jsx, src/contexts/CarbonContext.jsx]
autonomous: true
requirements: [AUTH-01, PROF-01, ACT-01]
must_haves:
  truths:
    - "AuthContext provides currentUser and userProfile to the app"
    - "CarbonContext tracks activities and calculates CO2 totals"
  artifacts:
    - path: "src/contexts/AuthContext.jsx"
      provides: "Firebase Auth and Profile management"
    - path: "src/contexts/CarbonContext.jsx"
      provides: "Carbon footprint data state and operations"
---

<objective>
Implement the core application contexts for Authentication and Carbon tracking.
Purpose: Centralize state management for user identity and environmental data, enabling reactive updates across the entire application.
Output: AuthContext.jsx and CarbonContext.jsx in src/contexts/.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
@src/lib/firebase.js
@src/config/carbonFactors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AuthContext</name>
  <files>src/contexts/AuthContext.jsx</files>
  <action>
    - Create src/contexts/AuthContext.jsx.
    - Setup Firebase Auth listener with onAuthStateChanged.
    - States: currentUser, userProfile (from Firestore), loading.
    - Functions: signup(email, password, name), login(email, password), loginWithGoogle(), logout(), updateProfile(data).
    - Logic: On signup/Google-first-login, create a user profile in 'users' collection with { name, email, totalSaved: 0, streak: 0, level: 'Seedling', joinedAt, onboardingComplete: false }.
    - Export useAuth() custom hook.
  </action>
  <verify>
    <automated>npm run lint && grep "AuthContext" src/contexts/AuthContext.jsx</automated>
  </verify>
  <done>AuthContext implemented with Firebase integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CarbonContext</name>
  <files>src/contexts/CarbonContext.jsx</files>
  <action>
    - Create src/contexts/CarbonContext.jsx.
    - States: activities, goals, insights, weeklyTotal, monthlyTotal, loading.
    - Functions: addActivity(data), fetchActivities(), fetchGoals(), addGoal(data), toggleGoal(goalId), fetchInsights().
    - Logic: weeklyTotal and monthlyTotal should be computed from the 'activities' state using factors from 'src/config/carbonFactors.js'.
    - Use useEffect to auto-fetch data when currentUser from AuthContext changes.
    - Export useCarbonContext() custom hook.
  </action>
  <verify>
    <automated>npm run lint && grep "CarbonContext" src/contexts/CarbonContext.jsx</automated>
  </verify>
  <done>CarbonContext implemented for data management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Firestore | Firestore Security Rules (enforced server-side) must protect user documents |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | AuthContext | mitigate | Ensure sensitive user data is not exposed in public profile fields |
</threat_model>

<verification>
Check that AuthContext correctly initializes and attempts to fetch a profile if a user is logged in.
</verification>

<success_criteria>
Both contexts are implemented, correctly wrap their logic, and handle the loading states.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-02-SUMMARY.md when done
</output>
