---
phase: 02-user-identity
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx, src/app/api/login/route.ts, src/app/api/logout/route.ts, src/components/auth/AuthProvider.tsx]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "User can sign up and login via Firebase Client SDK"
    - "Successful login sets a secure session cookie via /api/login"
    - "Logout clears the session cookie via /api/logout"
  artifacts:
    - path: "src/app/api/login/route.ts"
      provides: "Session bridge for token-to-cookie exchange"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "User login interface"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "/api/login"
      via: "POST request after client-side sign-in"
---

<objective>
Implement the user-facing authentication flows and bridge the client-side Firebase Auth with the server-side session.
Purpose: Allow users to create accounts and sign in securely.
Output: Login/Signup pages and session bridge API routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
@.planning/phases/02-user-identity/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Signup and Login UI</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/signup/page.tsx</files>
  <action>
    - Create responsive Login and Signup pages using Tailwind CSS 4.
    - Follow nature-inspired design: use 'bg-surface', 'text-primary', and 'earth' accents (D-04).
    - Use Firebase Client SDK ('signInWithEmailAndPassword', 'createUserWithEmailAndPassword') for initial authentication.
    - Include basic form validation (email format, password length).
  </action>
  <verify>
    <automated>npm run lint && ls src/app/(auth)/login/page.tsx src/app/(auth)/signup/page.tsx</automated>
  </verify>
  <done>Signup and Login pages implemented with Tailwind 4 styling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Session Bridge API and Client Observer</name>
  <files>src/app/api/login/route.ts, src/app/api/logout/route.ts, src/components/auth/AuthObserver.tsx</files>
  <action>
    - Create src/app/api/login/route.ts: Use next-firebase-auth-edge to verify the ID token and set the session cookie (D-01).
    - Create src/app/api/logout/route.ts: Use next-firebase-auth-edge to clear the session cookie.
    - Create src/components/auth/AuthObserver.tsx (Client Component):
      - Use 'onIdTokenChanged' from Firebase Client SDK.
      - When a new ID token is available, POST it to '/api/login'.
      - Refresh the page or redirect to '/profile' on success.
    - Include 'AuthObserver' in the root layout or a global provider.
  </action>
  <verify>
    <automated>npm run lint && grep "next-firebase-auth-edge" src/app/api/login/route.ts</automated>
  </verify>
  <done>Session bridge routes and client-side auth observer implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | ID token transmission; API must verify token before setting session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Session Bridge | mitigate | Verify Firebase ID token using next-firebase-auth-edge's verification logic |
</threat_model>

<verification>
Verify that after signing in via the Login page, a POST request to /api/login is made and a session cookie is set in the browser.
</verification>

<success_criteria>
User can login/signup, session is persisted across page refreshes via cookies.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-02-SUMMARY.md when done
</output>
