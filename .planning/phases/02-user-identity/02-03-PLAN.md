---
phase: 02-user-identity
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/app/profile/page.tsx, src/lib/actions/profile.ts, src/lib/schemas/profile.ts, src/lib/db/profile.ts]
autonomous: true
requirements: [PROF-01, PROF-02]
must_haves:
  truths:
    - "A Firestore user profile is automatically created on first signup"
    - "User can view their profile data on the /profile page"
    - "User can update their profile information via Server Actions"
  artifacts:
    - path: "src/lib/actions/profile.ts"
      provides: "Server Actions for profile management"
    - path: "src/app/profile/page.tsx"
      provides: "Profile management UI"
  key_links:
    - from: "src/lib/actions/profile.ts"
      to: "firebase-admin"
      via: "getFirestore().collection('users')"
---

<objective>
Manage user profile data in Firestore using React 19 Server Actions and provide a profile management interface.
Purpose: Allow users to personalize their experience and store environmental tracking data.
Output: Profile management page and associated Server Actions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
@.planning/phases/02-user-identity/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile Schema and Server Actions</name>
  <files>src/lib/schemas/profile.ts, src/lib/actions/profile.ts</files>
  <action>
    - Define Zod schema for user profile (name, email, preferences) in src/lib/schemas/profile.ts.
    - Implement 'updateProfile' Server Action in src/lib/actions/profile.ts:
      - Use 'useActionState' compatible signature.
      - Validate input with Zod.
      - Update Firestore document under 'users/{uid}' using Firebase Admin SDK (D-02, D-03).
      - Revalidate '/profile' path.
    - Implement 'getProfile' helper using Admin SDK.
  </action>
  <verify>
    <automated>npm run lint && grep "use server" src/lib/actions/profile.ts</automated>
  </verify>
  <done>Profile schema and Server Actions for updates implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-Profile Creation and Profile Page</name>
  <files>src/app/api/login/route.ts, src/app/profile/page.tsx</files>
  <action>
    - Update src/app/api/login/route.ts: After setting the session cookie, check if a Firestore profile document exists for the UID. If not, create a default one (PROF-01).
    - Create src/app/profile/page.tsx (Server Component):
      - Fetch current user profile using Admin SDK.
      - Pass data to a ProfileForm (Client Component).
    - Implement ProfileForm using 'useActionState' and the 'updateProfile' action.
    - Style with Tailwind CSS 4, maintaining the nature-inspired theme (D-04).
  </action>
  <verify>
    <automated>npm run lint && ls src/app/profile/page.tsx</automated>
  </verify>
  <done>Automatic profile creation integrated and Profile page implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Firestore | Admin SDK has full access; Server Action must verify UID from session cookie before mutating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | Profile Update | mitigate | Server Action must extract UID from secure session cookie/Middleware header, not client-provided UID |
</threat_model>

<verification>
Log in with a new user and verify that a document is created in the 'users' collection in Firestore. Then, update the name on the /profile page and verify the change persists.
</verification>

<success_criteria>
Automatic profile creation on signup, profile editing functional via Server Actions.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-03-SUMMARY.md when done
</output>
