---
phase: 02-user-identity
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/components/layout/AppLayout.jsx, src/App.jsx, src/components/ProtectedRoute.jsx]
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "App layout displays a sidebar on desktop and bottom nav on mobile"
    - "App routing correctly handles protected and public routes"
  artifacts:
    - path: "src/components/layout/AppLayout.jsx"
      provides: "Main navigation and structure for authenticated users"
    - path: "src/App.jsx"
      provides: "Central routing and provider configuration"
---

<objective>
Establish the application's structural layout and routing logic.
Purpose: Provide a consistent navigation interface and ensure that only authorized users can access specific pages.
Output: AppLayout.jsx, ProtectedRoute.jsx, and an updated App.jsx.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-identity/02-CONTEXT.md
@src/contexts/AuthContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppLayout and ProtectedRoute</name>
  <files>src/components/layout/AppLayout.jsx, src/components/ProtectedRoute.jsx</files>
  <action>
    - Create src/components/layout/ folder.
    - AppLayout.jsx: Implement a responsive layout. Desktop: Fixed left sidebar with icons (lucide-react) for Dashboard, Tracker, AI Coach, Insights, Goals, Leaderboard, Tips, Profile. Mobile: Fixed bottom navigation bar. Sidebar footer should show user avatar, name, and level badge.
    - ProtectedRoute.jsx: A wrapper component that checks useAuth(). If loading, show Spinner. If not logged in, redirect to /login. If logged in but !userProfile.onboardingComplete and the path isn't /onboarding, redirect to /onboarding.
  </action>
  <verify>
    <automated>npm run lint && ls src/components/layout/AppLayout.jsx src/components/ProtectedRoute.jsx</automated>
  </verify>
  <done>Layout and protection logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Configure App Routing in App.jsx</name>
  <files>src/App.jsx</files>
  <action>
    - Update src/App.jsx to use React Router v6.
    - Wrap the entire app with: AuthProvider, CarbonProvider, Toaster (react-hot-toast), and Router.
    - Define routes:
      - Public: /login, /signup, /forgot-password.
      - Protected (via ProtectedRoute): /dashboard, /tracker, /insights, /goals, /leaderboard, /tips, /coach, /profile, /onboarding.
    - Set the '/' route to redirect based on auth status.
    - Wrap protected page routes with <AppLayout>.
  </action>
  <verify>
    <automated>npm run lint && grep "Routes" src/App.jsx</automated>
  </verify>
  <done>App.jsx updated with providers and comprehensive routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client-side Router | Routing is for UX; actual data security must be enforced by Firestore Security Rules |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation of Privilege | ProtectedRoute | mitigate | Ensure the client-side check is robust, but always back it with server-side rules |
</threat_model>

<verification>
Check that accessing /dashboard while logged out redirects to /login.
</verification>

<success_criteria>
The application has a functional shell (Sidebar/Nav) and correct routing behavior for both authenticated and unauthenticated states.
</success_criteria>

<output>
Create .planning/phases/02-user-identity/02-03-SUMMARY.md when done
</output>
