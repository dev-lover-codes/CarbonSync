---
phase: 04-goals-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/types/index.ts, src/lib/goals.ts]
autonomous: true
requirements: [GOAL-01, GOAL-02]

must_haves:
  truths:
    - "Goal data is correctly shaped and typed for TypeScript"
    - "Firestore helper functions can save, fetch, and update goal documents"
    - "Progress calculation logic accurately sums activity impact within a date range"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Goal and Activity interfaces"
    - path: "src/lib/goals.ts"
      provides: "Firestore helpers and progress calculation"
  key_links:
    - from: "src/lib/goals.ts"
      to: "Firestore"
      via: "firebase/firestore SDK"
---

<objective>
Setup the foundational data layer for Goal management, including dependencies, type definitions, and Firestore integration logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-goals-management/04-RESEARCH.md
@.planning/phases/04-goals-management/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and define core types</name>
  <files>package.json, src/types/index.ts</files>
  <action>
    1. Install required packages: `lucide-react`, `recharts`, `framer-motion`, `date-fns`, `firebase`.
    2. Create `src/types/index.ts` and define `Goal` and `Activity` interfaces.
    3. `Goal` should include: `id`, `userId`, `title`, `targetValue` (kg CO2e), `category` ('all' or specific), `startDate`, `endDate`, `status` ('active', 'achieved', 'failed').
    4. `Activity` should include: `id`, `userId`, `category`, `co2e` (kg), `timestamp`.
  </action>
  <verify>
    <automated>npm list lucide-react recharts framer-motion date-fns firebase && npx tsc --noEmit</automated>
  </verify>
  <done>Dependencies installed and type definitions exported without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore goal helpers</name>
  <files>src/lib/goals.ts</files>
  <action>
    Implement Firestore CRUD helpers in `src/lib/goals.ts`:
    - `saveGoal(userId: string, goal: Omit<Goal, 'id'>): Promise<string>`
    - `getGoals(userId: string): Promise<Goal[]>`
    - `updateGoal(goalId: string, updates: Partial<Goal>): Promise<void>`
    Use a sub-collection pattern: `users/{userId}/goals`.
    Ensure `serverTimestamp()` is used for `createdAt` and `updatedAt`.
  </action>
  <verify>
    <automated># Manual verification via script or test if vitest is ready, otherwise check export signatures
    grep -E "export const (saveGoal|getGoals|updateGoal)" src/lib/goals.ts
    </automated>
  </verify>
  <done>Firestore helper functions implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 3: Implement progress calculation logic</name>
  <files>src/lib/goals.ts</files>
  <action>
    Add `calculateGoalProgress(activities: Activity[], goal: Goal): number` to `src/lib/goals.ts`.
    Logic:
    - Filter activities by goal date range (`startDate` to `endDate`).
    - Filter by category if goal is not 'all'.
    - Sum the `co2e` values.
    - Return the total kg CO2e.
    Use `date-fns` for robust date comparisons (e.g., `isWithinInterval`).
  </action>
  <verify>
    <automated>grep "export const calculateGoalProgress" src/lib/goals.ts</automated>
  </verify>
  <done>Progress calculation logic implemented and handles date ranges correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Firestore | Untrusted client-side code interacts with database |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Firestore Goals | mitigate | Security Rules: `allow read: if auth.uid == userId` (implemented in later phase/rules config) |
| T-04-02 | Tampering | Goal Values | mitigate | Security Rules: Validate `targetValue` is positive number |
</threat_model>

<verification>
Ensure all `src/lib/goals.ts` exports match the research recommendations and handle units (kg CO2e) consistently.
</verification>

<success_criteria>
- Firestore helpers are ready for integration.
- Type system enforces Goal/Activity structure.
- Progress logic is unit-tested (or verified by inspection).
</success_criteria>

<output>
Create `.planning/phases/04-goals-management/04-01-SUMMARY.md` when done
</output>
