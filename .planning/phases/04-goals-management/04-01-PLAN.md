---
phase: 04-goals-management
plan: 01
type: execute
wave: 1
depends_on: ['01-02', '02-01', '03-01']
files_modified: [src/types/goals.ts, src/lib/goals.ts, src/lib/goals.test.ts, src/lib/firestore.js]
autonomous: true
requirements: [GOAL-01, GOAL-02]

must_haves:
  truths:
    - "Goal data is correctly typed for TypeScript"
    - "Firestore CRUD helpers are fully implemented and functional"
    - "Progress calculation logic accurately sums activity impact within a date range"
  artifacts:
    - path: "src/types/goals.ts"
      provides: "Goal interface"
    - path: "src/lib/goals.ts"
      provides: "Progress calculation logic"
    - path: "src/lib/firestore.js"
      provides: "Implemented Goal CRUD helpers (saveGoal, getGoals, updateGoal)"
    - path: "src/lib/goals.test.ts"
      provides: "Unit tests for goal logic"
---

<objective>
Setup the domain-specific logic for Goal management, including Firestore CRUD implementation and progress calculation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-goals-management/04-RESEARCH.md
@.planning/phases/04-goals-management/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test harness for goal logic</name>
  <files>src/lib/goals.test.ts</files>
  <action>
    Create \`src/lib/goals.test.ts\` with test cases for:
    - \`calculateGoalProgress\` with various date ranges and categories.
    - Mocked Firestore helpers (\`saveGoal\`, \`getGoals\`).
  </action>
  <verify>
    <automated>ls src/lib/goals.test.ts</automated>
  </verify>
  <done>Test harness created.</done>
</task>

<task type="auto">
  <name>Task 1: Define Goal types</name>
  <files>src/types/goals.ts</files>
  <action>
    Create \`src/types/goals.ts\`:
    - Import \`Activity\` from \`src/types/activities.ts\`.
    - Define and export \`Goal\` interface.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Goal types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore CRUD helpers</name>
  <files>src/lib/firestore.js</files>
  <action>
    Flesh out the placeholder functions in \`src/lib/firestore.js\`:
    - \`saveGoal\`: Write to \`users/{userId}/goals\`.
    - \`getGoals\`: Fetch all goals for a user.
    - \`updateGoal\`: Update a specific goal document.
    Ensure they use the \`db\` instance from \`src/lib/firebase.js\`.
  </action>
  <verify>
    <automated>grep -A 5 "export const saveGoal" src/lib/firestore.js</automated>
  </verify>
  <done>Firestore CRUD helpers implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement progress calculation logic</name>
  <files>src/lib/goals.ts</files>
  <action>
    Implement \`calculateGoalProgress(activities: Activity[], goal: Goal): number\` in \`src/lib/goals.ts\`.
    Use \`date-fns\` for interval comparisons.
  </action>
  <verify>
    <automated>npm test src/lib/goals.test.ts</automated>
  </verify>
  <done>Progress calculation logic implemented and verified.</done>
</task>

</tasks>
