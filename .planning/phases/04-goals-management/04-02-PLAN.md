---
phase: 04-goals-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/components/goals/GoalCard.tsx, src/components/goals/GoalForm.tsx, src/components/goals/GoalList.tsx, src/app/goals/page.tsx]
autonomous: true
requirements: [GOAL-01, GOAL-02]

must_haves:
  truths:
    - "Users can see a visual progress ring for each goal"
    - "Users can open a form to create or edit a goal"
    - "The goals dashboard updates automatically when goals are added"
  artifacts:
    - path: "src/components/goals/GoalCard.tsx"
      provides: "Visual progress representation using Recharts"
    - path: "src/components/goals/GoalForm.tsx"
      provides: "Form for goal creation and modification"
    - path: "src/app/goals/page.tsx"
      provides: "Main Goals Dashboard view"
  key_links:
    - from: "src/app/goals/page.tsx"
      to: "src/lib/goals.ts"
      via: "hook or direct call to getGoals"
---

<objective>
Build the interactive User Interface for setting, tracking, and managing carbon reduction goals.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-goals-management/04-RESEARCH.md
@.planning/phases/04-goals-management/04-CONTEXT.md
@.planning/phases/04-goals-management/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoalCard and GoalForm components</name>
  <files>src/components/goals/GoalCard.tsx, src/components/goals/GoalForm.tsx</files>
  <action>
    1. Create `GoalCard.tsx`:
       - Use `recharts` (PieChart/Pie) to create a progress ring.
       - Display title, target value, and calculated percentage.
       - Add a "Edit" button that triggers the form.
    2. Create `GoalForm.tsx`:
       - Use `framer-motion` for a smooth modal transition.
       - Include inputs for: Title, Target (kg CO2e), Category (Dropdown: All, Transport, Food, etc.), Start Date, and End Date.
       - Use `date-fns` for date input handling.
       - Integrate with `saveGoal` and `updateGoal` from `src/lib/goals.ts`.
  </action>
  <verify>
    <automated># Check for imports and component definitions
    grep "ResponsiveContainer" src/components/goals/GoalCard.tsx
    grep "motion" src/components/goals/GoalForm.tsx
    </automated>
  </verify>
  <done>UI components created with required interactive elements and visualization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Goals Dashboard page</name>
  <files>src/components/goals/GoalList.tsx, src/app/goals/page.tsx</files>
  <action>
    1. Create `GoalList.tsx`:
       - A grid component that maps over goals and renders `GoalCard` for each.
       - Handle empty states with a friendly "No goals set" message.
    2. Create `src/app/goals/page.tsx`:
       - Setup a protected page (assume Firebase Auth session is available).
       - Fetch goals using `getGoals` (and optionally activities to calculate progress).
       - Provide a "Set New Goal" button that opens the `GoalForm`.
       - Style the layout using Tailwind CSS with clean spacing and premium feel (per GEMINI.md guidelines).
  </action>
  <verify>
    <automated>ls src/app/goals/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Goals Dashboard page implemented and integrated with the data layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Client Logic | Data enters the system via forms |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | GoalForm | mitigate | Client-side validation for numeric inputs and valid date ranges. |
</threat_model>

<verification>
Manual verification required to ensure Recharts rendering is responsive and the form correctly updates Firestore.
</verification>

<success_criteria>
- Users can create, view, and edit goals.
- Progress visualization accurately reflects the (simulated or real) activity data.
- UI follows the project's aesthetic standards (Tailwind, animations).
</success_criteria>

<output>
Create `.planning/phases/04-goals-management/04-02-SUMMARY.md` when done
</output>
