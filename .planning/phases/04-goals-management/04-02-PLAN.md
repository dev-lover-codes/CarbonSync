---
phase: 04-goals-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/components/goals/GoalCard.tsx, src/components/goals/GoalForm.tsx, src/components/goals/GoalList.tsx, src/pages/Goals.tsx, src/components/goals/__tests__/GoalCard.test.tsx, src/components/goals/__tests__/GoalForm.test.tsx]
autonomous: true
requirements: [GOAL-01, GOAL-02]

must_haves:
  truths:
    - "Users can see a visual progress ring for each goal"
    - "Users can open a form to create or edit a goal"
    - "The goals dashboard updates automatically when goals are added"
  artifacts:
    - path: "src/components/goals/GoalCard.tsx"
      provides: "Visual progress representation using Recharts"
    - path: "src/components/goals/GoalForm.tsx"
      provides: "Form for goal creation and modification"
    - path: "src/pages/Goals.tsx"
      provides: "Main Goals Dashboard view (Vite Page)"
  key_links:
    - from: "src/pages/Goals.tsx"
      to: "src/lib/firestore.js"
      via: "getGoals helper"
    - from: "src/components/goals/GoalCard.tsx"
      to: "src/lib/goals.ts"
      via: "calculateGoalProgress"
---

<objective>
Build the interactive User Interface for setting, tracking, and managing carbon reduction goals using React + Vite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-goals-management/04-RESEARCH.md
@.planning/phases/04-goals-management/04-CONTEXT.md
@.planning/phases/04-goals-management/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create UI test files</name>
  <files>src/components/goals/__tests__/GoalCard.test.tsx, src/components/goals/__tests__/GoalForm.test.tsx</files>
  <action>
    Create test files using React Testing Library to verify:
    - \`GoalCard\` renders progress and title.
    - \`GoalForm\` handles input changes and validation.
  </action>
  <verify>
    <automated>ls src/components/goals/__tests__/GoalCard.test.tsx</automated>
  </verify>
  <done>UI test files created.</done>
</task>

<task type="auto">
  <name>Task 1: Create GoalCard and GoalForm components</name>
  <files>src/components/goals/GoalCard.tsx, src/components/goals/GoalForm.tsx</files>
  <action>
    1. Create \`GoalCard.tsx\`:
       - Use \`recharts\` (PieChart/Pie) to create a progress ring.
       - Display title, target value, and calculated percentage.
       - Add a "Edit" button that triggers the form.
    2. Create \`GoalForm.tsx\`:
       - Use \`framer-motion\` for a smooth modal transition.
       - Include inputs for: Title, Target (kg CO2e), Category (Dropdown), Start Date, and End Date.
       - Integrate with \`saveGoal\` and \`updateGoal\` from \`src/lib/firestore.js\`.
  </action>
  <verify>
    <automated>npm test src/components/goals/__tests__/GoalCard.test.tsx && npm test src/components/goals/__tests__/GoalForm.test.tsx</automated>
  </verify>
  <done>UI components created and verified with unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Goals Dashboard page</name>
  <files>src/components/goals/GoalList.tsx, src/pages/Goals.tsx</files>
  <action>
    1. Create \`GoalList.tsx\`:
       - A grid component that maps over goals and renders \`GoalCard\` for each.
    2. Create \`src/pages/Goals.tsx\`:
       - A standard React component for the Goals page.
       - Use \`useAuth\` (or equivalent from Phase 2) to get current user.
       - Fetch goals using \`getGoals\` from \`src/lib/firestore.js\`.
       - Provide a "Set New Goal" button.
       - Style using Tailwind CSS.
  </action>
  <verify>
    <automated>ls src/pages/Goals.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Goals Dashboard page implemented in the Vite pages directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Client Logic | Data validation in forms |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | GoalForm | mitigate | Validation for numeric inputs. |
</threat_model>
