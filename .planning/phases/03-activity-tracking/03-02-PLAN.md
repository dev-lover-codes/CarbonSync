---
phase: 03-activity-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/activities.ts, src/lib/constants.ts, blueprint.md]
autonomous: true
requirements: [ACT-01, ACT-03]

must_haves:
  truths:
    - "Activities are saved to user-specific Firestore sub-collections using 'timestamp' field"
    - "Recent activities are fetched in descending chronological order by 'timestamp'"
  artifacts:
    - path: "src/lib/activities.ts"
      provides: "Firestore CRUD helpers for activity logging and retrieval"
    - path: "src/lib/constants.ts"
      provides: "Project-wide constants including MOCK_USER_ID"
  key_links:
    - from: "src/lib/activities.ts"
      to: "Firestore"
      via: "firebase/firestore SDK"
---

<objective>
Implement the data persistence layer for activities using Firestore.
Purpose: Enable reliable storage and retrieval of user activity logs, facilitating historical views and future progress tracking.
Output: Firestore helper functions for activities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-02-PLAN.md
@src/lib/firebase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define mock user strategy</name>
  <files>src/lib/constants.ts</files>
  <action>
    - Create `src/lib/constants.ts` (if it doesn't exist).
    - Export `MOCK_USER_ID = 'user-123'`.
    - This ensures consistency while Phase 2 (Auth) is pending.
  </action>
  <verify>
    <automated>grep "MOCK_USER_ID" src/lib/constants.ts</automated>
  </verify>
  <done>Mock user ID defined for development.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore activity helpers</name>
  <files>src/lib/activities.ts</files>
  <action>
    - Import `db` from `src/lib/firebase`.
    - Implement `logActivity(userId: string, data: ActivityInput)`:
      - Adds a document to `users/{userId}/activities`.
      - Adds `serverTimestamp()` for the `timestamp` field (matching schemas).
    - Implement `getRecentActivities(userId: string, limitCount = 10)`:
      - Queries `users/{userId}/activities` ordered by `timestamp` DESC.
      - Returns an array of activity objects with IDs, converting Firestore `timestamp` (Timestamp) to JS Date via `.toDate()`.
  </action>
  <verify>
    <automated>grep -E "logActivity|getRecentActivities" src/lib/activities.ts && grep "timestamp" src/lib/activities.ts</automated>
  </verify>
  <done>Firestore helpers implemented and exported correctly using standardized 'timestamp' field.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize and lint</name>
  <files>blueprint.md, src/lib/activities.ts</files>
  <action>
    - Run `npm run lint -- --fix`.
    - Update `blueprint.md` with descriptions of the new Firestore helper functions.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Code is linted and blueprint is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> Firestore | Data is sent to a cloud database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | Firestore | mitigate | Use per-user sub-collections. Access control rules (handled in Auth phase) must restrict access to own sub-collection. |
</threat_model>

<verification>
Exports logActivity and getRecentActivities; uses Firestore SDK correctly.
</verification>

<success_criteria>
Helper functions correctly handle the sub-collection path structure `users/{userId}/activities`.
</success_criteria>

<output>
Create .planning/phases/03-activity-tracking/03-02-SUMMARY.md when done
</output>
