---
phase: 03-activity-tracking
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [src/components/activities/ActivityForm.tsx, src/components/activities/ActivityList.tsx, src/app/activities/log/page.tsx, src/app/activities/page.tsx]
autonomous: true
requirements: [ACT-01, ACT-03]

must_haves:
  truths:
    - "User can log a new transport activity via the UI"
    - "Recent activities are displayed in a list with their CO2 impact"
  artifacts:
    - path: "src/components/activities/ActivityForm.tsx"
      provides: "Dynamic form for logging different activity types"
    - path: "src/components/activities/ActivityList.tsx"
      provides: "Display component for activity history"
    - path: "src/app/activities/page.tsx"
      provides: "Dashboard view for activities"
  key_links:
    - from: "src/components/activities/ActivityForm.tsx"
      to: "src/lib/calculations.ts"
      via: "real-time impact calculation"
    - from: "src/components/activities/ActivityForm.tsx"
      to: "src/lib/activities.ts"
      via: "logActivity call on submit"
---

<objective>
Create the user interface for logging activities and viewing history.
Purpose: Provide an intuitive and engaging experience for users to track their environmental impact in real-time.
Output: Integrated UI components and pages for activity tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-activity-tracking/03-RESEARCH.md
@.planning/phases/03-activity-tracking/03-01-SUMMARY.md
@.planning/phases/03-activity-tracking/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Activity Logging Form</name>
  <files>src/components/activities/ActivityForm.tsx</files>
  <action>
    - Create a multi-category form using `react-hook-form` and `zod`.
    - Category selection: Buttons/Icons for Transport, Energy, Food, Shopping, Waste.
    - Dynamic fields:
      - Transport: Distance (km), Mode (Car, Bus, Train, Flight).
      - Energy: Usage (kWh), Source (Electricity, Gas).
      - Food: Diet type or specific items.
    - Real-time preview: Show calculated CO2e impact using `src/lib/calculations.ts` as the user types.
    - On Submit: Call `logActivity` with current user's ID (mock if Phase 2 not finished).
  </action>
  <verify>
    <automated>grep -E "useForm|ActivitySchema" src/components/activities/ActivityForm.tsx</automated>
  </verify>
  <done>Interactive activity form with real-time feedback and submission logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Activity List and History View</name>
  <files>src/components/activities/ActivityList.tsx, src/app/activities/page.tsx</files>
  <action>
    - Create `ActivityList` component:
      - Maps over an array of activities.
      - Displays category icon (`lucide-react`), sub-type, value, timestamp (relative time via `date-fns`), and CO2e impact.
    - Create `src/app/activities/page.tsx`:
      - Fetches recent activities for the current user using `getRecentActivities`.
      - Renders `ActivityList`.
      - Includes a "Log New Activity" button linking to `/activities/log`.
  </action>
  <verify>
    <automated>ls src/app/activities/page.tsx && grep "ActivityList" src/app/activities/page.tsx</automated>
  </verify>
  <done>Activity history dashboard and list component implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Log Activity Page</name>
  <files>src/app/activities/log/page.tsx</files>
  <action>
    - Create `src/app/activities/log/page.tsx`.
    - Render `ActivityForm`.
    - Add a back button to the main activities page.
    - Style with Tailwind CSS (nature-inspired palette per PROJECT.md).
  </action>
  <verify>
    <automated>ls src/app/activities/log/page.tsx</automated>
  </verify>
  <done>Log activity page created and styled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Form | User inputs data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | ActivityForm | mitigate | Client-side Zod validation + (Phase 2) server-side validation/rules. |
</threat_model>

<verification>
Pages load without errors; Form calculates impact; List displays data.
</verification>

<success_criteria>
End-to-end flow: User can navigate to /activities/log, fill out a form, see the impact, submit, and see the new entry in the history list at /activities.
</success_criteria>

<output>
Create .planning/phases/03-activity-tracking/03-03-SUMMARY.md when done
</output>
