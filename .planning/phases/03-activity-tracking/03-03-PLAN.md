---
phase: 03-activity-tracking
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [src/components/activities/ActivityForm.tsx, src/components/activities/ActivityList.tsx, src/app/activities/log/page.tsx, src/app/activities/page.tsx, src/lib/actions/activities.ts, blueprint.md]
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "User can log a new transport activity via the UI"
    - "Recent activities are displayed in a list with their CO2 impact"
    - "CO2 impact is calculated and shown in real-time on the form"
    - "Data mutations use Server Actions for improved security and performance"
  artifacts:
    - path: "src/components/activities/ActivityForm.tsx"
      provides: "Dynamic form for logging different activity types"
    - path: "src/lib/actions/activities.ts"
      provides: "Server Actions for activity logging using Firebase Admin SDK"
  key_links:
    - from: "src/app/activities/page.tsx"
      to: "src/lib/activities.ts"
      via: "getRecentActivities call (Server-side fetching)"
    - from: "src/components/activities/ActivityForm.tsx"
      to: "src/lib/actions/activities.ts"
      via: "logActivityAction call (Server Action)"
---

<objective>
Build the user interface for activity tracking, integrating real-time calculations and server-side persistence.
Purpose: Provide users with an intuitive way to log their impact and view their history while adhering to Next.js best practices.
Output: Functional activity logging forms, history list, and server actions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-activity-tracking/03-RESEARCH.md
@src/lib/calculations.ts
@src/lib/activities.ts
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Server Action for activity logging</name>
  <files>src/lib/actions/activities.ts</files>
  <action>
    - Create `src/lib/actions/activities.ts` with `"use server"` directive.
    - Implement `logActivityAction(formData: FormData)`:
      - Extract and validate data using Zod schemas from `src/types/activities.ts`.
      - Calculate CO2e server-side using `src/lib/calculations.ts` for final validation.
      - Save to Firestore using a server-safe Firebase initialization (Admin SDK or server-side Client SDK as configured in Phase 1).
      - Revalidate the `/activities` path.
  </action>
  <verify>
    <automated>grep "'use server'" src/lib/actions/activities.ts</automated>
  </verify>
  <done>Server Action for logging activities is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build ActivityForm with real-time calculations</name>
  <files>src/components/activities/ActivityForm.tsx</files>
  <action>
    - Use `react-hook-form` and `zodResolver`.
    - Implement a category selector.
    - Use `watch()` to trigger `calculateTransport` (etc.) and display estimated CO2 in real-time.
    - On submit, use the `logActivityAction` Server Action.
  </action>
  <verify>
    <automated>grep "logActivityAction" src/components/activities/ActivityForm.tsx</automated>
  </verify>
  <done>ActivityForm supports all categories with real-time feedback and server-side submission.</done>
</task>

<task type="auto">
  <name>Task 3: Build ActivityList and main activities page (RSC)</name>
  <files>src/components/activities/ActivityList.tsx, src/app/activities/page.tsx</files>
  <action>
    - `ActivityList`: Renders an array of activities with icons and formatted dates.
    - `src/app/activities/page.tsx` (Server Component):
      - Fetches data using `getRecentActivities(MOCK_USER_ID)` (Ensure helper is RSC-safe).
      - Renders the `ActivityList`.
  </action>
  <verify>
    <automated>grep "getRecentActivities" src/app/activities/page.tsx</automated>
  </verify>
  <done>Activities page renders server-side with historical data.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize and lint</name>
  <files>blueprint.md, src/app/activities/page.tsx</files>
  <action>
    - Run `npm run lint -- --fix`.
    - Update `blueprint.md` with the full overview of the Activity Tracking feature, including Server Actions logic.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Code is linted and blueprint is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server | Server Actions provide a clear boundary for validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Server Action | mitigate | Re-validate all inputs server-side using Zod. |
</threat_model>

<verification>
UI allows logging via Server Actions and displays history; real-time feedback works.
</verification>

<success_criteria>
A user can log a car trip via a Server Action and see the updated history after revalidation.
</success_criteria>

<output>
Create .planning/phases/03-activity-tracking/03-03-SUMMARY.md when done
</output>
