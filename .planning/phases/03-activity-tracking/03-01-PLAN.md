---
phase: 03-activity-tracking
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [package.json, src/types/activities.ts, src/lib/calculations.ts, src/lib/calculations.test.ts, blueprint.md]
autonomous: true
requirements: [ACT-01, ACT-02]

must_haves:
  truths:
    - "Calculation logic accurately converts km to CO2e for cars using carbon-footprint library"
    - "Activity schemas correctly validate transport, energy, food, shopping, and waste inputs"
  artifacts:
    - path: "src/types/activities.ts"
      provides: "Zod schemas and TypeScript types for all activity categories"
    - path: "src/lib/calculations.ts"
      provides: "Unit-safe CO2 calculation wrapper for carbon-footprint library"
  key_links:
    - from: "src/lib/calculations.ts"
      to: "carbon-footprint"
      via: "import and SI conversion logic"
---

<objective>
Implement a robust, unit-tested calculation engine and data schemas for environmental activities.
Purpose: Ensure all CO2 impact calculations are accurate, standardized, and type-safe before building UI or persistence.
Output: Validated schemas and a tested calculation library.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-activity-tracking/03-RESEARCH.md
@.planning/phases/03-activity-tracking/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and define activity schemas</name>
  <files>package.json, src/types/activities.ts</files>
  <action>
    - Install dependencies: `npm install react-hook-form zod carbon-footprint date-fns lucide-react`.
    - Install dev dependencies: `npm install -D vitest`.
    - Create `src/types/activities.ts`:
      - Define `ActivityCategory` enum: `transport`, `energy`, `food`, `shopping`, `waste`.
      - Define a base `ActivitySchema` using Zod (id, userId, category, timestamp, co2e).
      - Define category-specific input schemas (e.g., `TransportInputSchema` with `distanceKm`, `mode`).
      - Export inferred TypeScript types.
  </action>
  <verify>
    <automated>npm list carbon-footprint zod vitest && npx tsc --noEmit</automated>
  </verify>
  <done>Dependencies installed and type-safe schemas defined for all categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CO2 calculation engine (TDD)</name>
  <files>src/lib/calculations.ts, src/lib/calculations.test.ts</files>
  <behavior>
    - `calculateTransport(10, car)` -> returns ~1.7 (based on 0.17 factor if applicable, verify against carbon-footprint).
    - `calculateEnergy(100, electricity)` -> returns ~30 (based on 0.3 factor).
    - Handle SI conversions: Library uses meters/joules; wrapper must accept km/kWh.
  </behavior>
  <action>
    - RED: Create `src/lib/calculations.test.ts` with test cases for each category based on `carbon-footprint` constants.
    - GREEN: Implement `src/lib/calculations.ts` wrapping `carbon-footprint` functions.
    - Ensure all exports are unit-safe (accepting user-friendly units like km, kWh).
  </action>
  <verify>
    <automated>npx vitest run src/lib/calculations.test.ts</automated>
  </verify>
  <done>Tested calculation engine supports all required activity types with correct unit conversions.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize and lint</name>
  <files>blueprint.md, src/types/activities.ts, src/lib/calculations.ts</files>
  <action>
    - Run `npm run lint -- --fix` to ensure code style compliance.
    - Update `blueprint.md` with details of the new Activity schemas and calculation engine.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Code is linted and blueprint is updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logic -> Library | External dependency 'carbon-footprint' provides constants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | src/lib/calculations.ts | mitigate | Zod validation in Task 1 prevents malformed numeric inputs. |
| T-03-SC | Tampering | npm installs | mitigate | slopcheck identified `carbon-footprint` as [SUS] (low volume). Human verification of package source recommended. |

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Dependency installation including [SUS] package `carbon-footprint`</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/carbon-footprint
    2. Confirm it aligns with https://github.com/NMF-earth/carbon-footprint
    3. Confirm 1.7.0 is a valid version.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation</resume-signal>
</task>
</threat_model>

<verification>
npx vitest run src/lib/calculations.test.ts passes and npm run lint passes.
</verification>

<success_criteria>
Calculations are accurate to 2 decimal places against known emission factors.
</success_criteria>

<output>
Create .planning/phases/03-activity-tracking/03-01-SUMMARY.md when done
</output>
