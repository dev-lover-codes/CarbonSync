---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/lib/firebase.js, src/lib/firestore.js, src/config/carbonFactors.js, .env.local, .env.example]
autonomous: true
requirements: [INFRA-03, INFRA-04]
must_haves:
  truths:
    - "Firebase app is initialized without errors"
    - "Firebase auth and db (Firestore) are exported from src/lib/firebase.js"
    - "calculateFootprint([ { type: 'transport', value: 10 } ]) returns a positive numeric value"
  artifacts:
    - path: "src/lib/firebase.js"
      provides: "Firebase client initialization"
    - path: "src/config/carbonFactors.js"
      provides: "Carbon emission factors and calculation logic"
    - path: ".env.example"
      provides: "Template for required Firebase environment variables"
---

<objective>
Configure Firebase services (Auth and Firestore) and define the carbon calculation logic with specific emission factors.
Purpose: Provide the backend and business logic foundations for user authentication and environmental impact tracking.
Output: Initialized Firebase SDK and a reusable carbon calculation utility.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-01/01-CONTEXT.md
# Reference current plan 01 output if needed
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Firebase and Environment Variables</name>
  <files>src/lib/firebase.js, .env.local, .env.example</files>
  <read_first>
    - .planning/phase-01/01-CONTEXT.md
  </read_first>
  <action>
    - Install 'firebase' SDK.
    - Create .env.example with keys: VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, VITE_FIREBASE_PROJECT_ID, VITE_FIREBASE_STORAGE_BUCKET, VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID.
    - Create .env.local (copy from .env.example, user will fill values or the agent can use placeholders if testing).
    - Create src/lib/firebase.js:
      - Import initializeApp, getAuth, getFirestore from 'firebase'.
      - Initialize app using import.meta.env.VITE_FIREBASE_* variables.
      - Export app, auth (getAuth(app)), and db (getFirestore(app)).
  </action>
  <verify>
    <automated>npm list firebase && grep "import.meta.env.VITE_FIREBASE" src/lib/firebase.js</automated>
  </verify>
  <done>Firebase SDK installed and initialized with environment variables.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Firestore Helpers and Carbon Logic</name>
  <files>src/lib/firestore.js, src/config/carbonFactors.js</files>
  <read_first>
    - .planning/phase-01/01-CONTEXT.md
    - src/lib/firebase.js
  </read_first>
  <action>
    - Create src/lib/firestore.js:
      - Import db from ./firebase.
      - Add placeholder exports for future phases: createUserProfile, getUserProfile, logActivity, getActivities, getLeaderboard.
    - Create src/config/carbonFactors.js with the following factors (kg CO2 per unit):
      - transport: { car: 0.17, bus: 0.1, train: 0.04 } (per km)
      - energy: { electricity: 0.3, gas: 0.2 } (per kWh)
      - food: { meat: 7.0, vegetarian: 3.5, vegan: 2.0 } (per day)
      - shopping: { general: 0.5 } (per item)
      - waste: { general: 1.2 } (per kg)
    - Implement export function calculateFootprint(activities) in src/config/carbonFactors.js that sums the impact.
  </action>
  <verify>
    <automated>ls src/lib/firestore.js src/config/carbonFactors.js && grep "calculateFootprint" src/config/carbonFactors.js</automated>
  </verify>
  <done>Firestore helpers scaffolded and carbon calculation logic implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Firebase | Client SDK interacts directly with Firebase. Security rules must be enforced (Phase 2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | .env.local | mitigate | Ensure .env.local is in .gitignore (standard Vite setup). |
</threat_model>

<verification>
Verify Firebase initialization and carbon calculation output.
</verification>

<success_criteria>
- [ ] Firebase 'auth' and 'db' are exported correctly.
- [ ] calculateFootprint returns expected sums for sample activity arrays.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>
