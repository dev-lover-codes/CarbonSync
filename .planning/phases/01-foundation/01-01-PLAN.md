---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/index.css, GEMINI.md, README.md]
autonomous: false
requirements: [INFRA-01, INFRA-02]
must_haves:
  truths:
    - "Vite development server starts with 'npm run dev'"
    - "Tailwind CSS classes (e.g., bg-primary) apply styles to elements"
    - "Plus Jakarta Sans font is loaded and used in the application"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite configuration for React"
    - path: "src/index.css"
      provides: "Tailwind v4 configuration and theme variables"
    - path: "GEMINI.md"
      provides: "Updated AI guidelines for Vite/React environment"
---

<objective>
Scaffold the React + Vite foundation and configure Tailwind CSS with the nature-inspired theme (Primary, Earth, Sky, Surface).
Purpose: Migrate from Next.js to a faster, lighter Vite-based setup requested by the user for the hackathon.
Output: A working React + Vite application with Tailwind CSS and custom design system.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-01/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React and Clean Up Next.js</name>
  <files>package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, tsconfig.json</files>
  <read_first>
    - package.json
    - .planning/phase-01/01-CONTEXT.md
  </read_first>
  <action>
    - Update package.json:
      - Remove 'next', 'eslint-config-next'.
      - Add 'vite', '@vitejs/plugin-react', 'react-router-dom', 'lucide-react', 'framer-motion', 'react-hot-toast', 'recharts'.
      - Set scripts: "dev": "vite", "build": "tsc && vite build", "preview": "vite preview".
    - Create vite.config.ts using '@vitejs/plugin-react'.
    - Create index.html in root with a div#root and script tag pointing to src/main.tsx.
    - Create src/main.tsx and src/App.tsx (basic router/placeholder).
    - Update tsconfig.json to support Vite (target: ESNext, module: ESNext, types: ["vite/client"]).
    - Remove Next.js artifacts: src/app directory, next.config.ts, next-env.d.ts.
  </action>
  <verify>
    <automated>npm list vite && ls vite.config.ts index.html</automated>
  </verify>
  <done>Vite project scaffolded, package.json updated, and Next.js files removed.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 and Nature Theme</name>
  <files>src/index.css, src/App.tsx</files>
  <read_first>
    - .planning/phase-01/01-CONTEXT.md
  </read_first>
  <action>
    - Ensure tailwindcss and @tailwindcss/postcss (or standard v4 setup) are present.
    - In src/index.css, use @theme block to define custom variables per D-02 in 01-CONTEXT.md:
      - --color-primary: #2D6A4F, --color-primary-light: #52B788, --color-primary-dark: #1B4332
      - --color-earth: #8B5E3C, --color-earth-light: #D4A574, --color-earth-dark: #5C3D1E
      - --color-sky: #0EA5E9, --color-sky-light: #BAE6FD, --color-sky-dark: #0369A1
      - --color-surface: #F8FAF7, --color-surface-card: #FFFFFF, --color-surface-dark: #0F1C14
      - --font-plus-jakarta: "Plus Jakarta Sans", sans-serif
    - Import "Plus Jakarta Sans" via Google Fonts link in index.html.
    - Update src/App.tsx with a sample UI using these classes (e.g., bg-surface text-primary font-plus-jakarta).
  </action>
  <verify>
    <automated>grep "@theme" src/index.css && grep "Plus Jakarta Sans" index.html</automated>
  </verify>
  <done>Tailwind CSS v4 configured with custom nature-inspired theme and fonts.</done>
</task>

<task type="auto">
  <name>Task 3: Update AI Guidelines (GEMINI.md)</name>
  <files>GEMINI.md</files>
  <read_first>
    - GEMINI.md
  </read_first>
  <action>
    - Remove Next.js specific sections (App Router, Server Components, Server Actions).
    - Add Vite-specific guidance:
      - "The AI will monitor the output of the running Vite dev server."
      - "Project follows standard React + Vite structure (src/main.tsx, src/App.tsx)."
      - "Routing is handled via react-router-dom."
      - "Styling is handled via Tailwind CSS v4 in src/index.css."
  </action>
  <verify>
    <automated>grep "Vite" GEMINI.md && ! grep "next dev" GEMINI.md</automated>
  </verify>
  <done>GEMINI.md updated to reflect the Vite + React pivot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>React + Vite scaffold with Tailwind CSS nature theme.</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open the preview URL.
    3. Verify the "CarbonSync" placeholder is visible.
    4. Verify the background color matches the "surface" theme (#F8FAF7) and text is "primary" (#2D6A4F).
    5. Verify the font is "Plus Jakarta Sans".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | Browser environment, untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json | mitigate | Verify package legitimacy during install. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that Vite starts and Tailwind styles apply as expected.
</verification>

<success_criteria>
- [ ] Vite dev server runs without errors.
- [ ] Tailwind primary color (#2D6A4F) is applied and visible.
- [ ] GEMINI.md no longer mentions Next.js as the primary framework.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-01-SUMMARY.md when done
</output>
